from fakepinterest import app

if __name__  == '__main__': #colocando o site no ar
    app.run(debug= False)


#caso queira verificar todas as bibliotecas basta rodar um pip freeze, e caso queira salvar isso em um arquivo txt rode o comando com > requirements.txt

#comandos para atualizar, projeto no github para todas vez que, modificar o projeto.  git add . , git commit -m "adicionei requirements.txt", git push origin main